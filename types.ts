export type SubnameRow = {
  name: string;
  ethAddress: string;
};

export type UserRecord = {
  avatar: string;
  subname: string;
  address: string;
  description: string;
  location: string;
  twitter: string;
  website: string;
};
